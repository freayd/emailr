%table.listing
  %tr
    %th Visiteur
    %th Newsletter
    %th Loggé le
  - @logs.each do |log|
    %tr
      %td&= log.visitor_id
      %td=  link_to log.issue.newsletter.name, log.issue.newsletter
      %td&= log.created_at
